/* ========================================================================
   elements/stone.js — Stone (Indestructible Solid) Simulation Rules
   ========================================================================
   Stone is completely static, immovable, and indestructible by most things.
   We keep an empty module for structural consistency.
   ======================================================================== */

export function simulateStone(read, write, x, y, i, cell) {
  // Stone does nothing on its own tick.
}
